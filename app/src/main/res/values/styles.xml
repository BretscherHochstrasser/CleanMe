<resources>

    <!-- Base application theme -->
    <style name="AppTheme.Base" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- General theme customizations -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="colorPrimaryVariant">@color/primaryLightColor</item>
        <item name="colorSecondary">@color/secondaryColor</item>
        <item name="colorSecondaryVariant">@color/secondaryLightColor</item>
        <item name="colorOnPrimary">@color/onPrimaryColor</item>
        <item name="colorOnSecondary">@color/onSecondaryColor</item>
        <item name="colorError">@color/errorColor</item>
        <item name="android:textColorLink">@color/primaryColor</item>
    </style>

    <!-- The actual app theme, non-dark version -->
    <style name="AppTheme" parent="AppTheme.Base">
        <!-- no special customizations in non-dark mode -->
    </style>

    <!-- Custom WelcomeScreenTheme to override button texts for translations -->
    <style name="CleanMeWelcomeScreenTheme" parent="WelcomeScreenTheme">
        <item name="welcomeButtonDoneText">@string/welcome_button_done</item>
        <item name="welcomeButtonSkipText">@string/welcome_button_skip</item>
    </style>

    <!-- Override TimeDurationPickerDialog's style to apply primary color to duration background -->
    <style name="Widget.TimeDurationPicker.Dialog">
        <item name="durationDisplayBackground">?colorPrimary</item>
        <item name="textAppearanceDisplay">@style/TextAppearance.TimeDurationPicker.Display.Large.Dark</item>
        <item name="textAppearanceUnit">@style/TextAppearance.TimeDurationPicker.Unit.Large.Dark</item>
    </style>

</resources>
